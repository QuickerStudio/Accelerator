# 设置页面导航界面设计 (Settings Pages)

## 设计思路

从"我的"页面点击功能设置项后，会进入对应的详细设置页面。每个设置页面都有统一的顶部导航栏，包含返回按钮和页面标题。页面内容根据功能不同而有所差异，但都遵循相同的设计规范和交互模式。

---

## 1. 朗读音色设置页面 (Voice Settings)

### 设计思路
用户可以选择不同的TTS（文本转语音）朗读音色，每种音色都可以试听。支持男声、女声、不同口音和语速调节。

### 布局结构
```
┌─────────────────────────────────┐
│  顶部导航栏                       │
│  ←  朗读音色                     │
├─────────────────────────────────┤
│                                 │
│  当前音色                        │
│  ┌─────────────────────────┐   │
│  │ 🔊 美式女声 (Emma)       │   │
│  │ 清晰自然，适合学习        │   │
│  │         [试听] [✓]      │   │
│  └─────────────────────────┘   │
│                                 │
│  推荐音色                        │
│  ┌─────────────────────────┐   │
│  │ 🔊 美式男声 (James)      │   │
│  │ 沉稳专业，发音标准        │   │
│  │         [试听]          │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 🔊 英式女声 (Sophie)     │   │
│  │ 优雅动听，英伦口音        │   │
│  │         [试听]          │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 🔊 英式男声 (Oliver)     │   │
│  │ 磁性低沉，标准英音        │   │
│  │         [试听]          │   │
│  └─────────────────────────┘   │
│                                 │
│  语速调节                        │
│  ┌─────────────────────────┐   │
│  │ 慢速 ●━━━━━━━━━━ 快速    │   │
│  │        1.0x             │   │
│  └─────────────────────────┘   │
│                                 │
│  音量调节                        │
│  ┌─────────────────────────┐   │
│  │ 小声 ━━━━━━━●━━━ 大声    │   │
│  │        80%              │   │
│  └─────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

### 设计细节

**顶部导航栏**
- 高度: 64.dp
- 背景: 深色 (#1E1E1E)
- 内边距: 水平16.dp
- 布局: 水平排列
- 组件:
  - **返回按钮** (←):
    - 尺寸: 48.dp × 48.dp
    - 图标: 24.dp, 白色
    - 点击: 返回上一页
  - **标题**:
    - 文本: "朗读音色", HeadlineMedium (20.sp), 白色
    - 位置: 返回按钮右侧

**当前音色卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 主题色渐变
- 内边距: 20.dp
- 边距: 水平16.dp, 顶部16.dp
- 组件:
  - **音色图标**: 🔊, 32.dp, 白色
  - **音色名称**: HeadlineMedium (18.sp), 白色, 粗体
  - **音色描述**: BodyMedium (14.sp), 白色透明80%
  - **试听按钮**:
    - 宽度: 80.dp, 高度: 36.dp
    - 圆角: CornerRadius.full
    - 背景: 白色透明20%
    - 文本: "试听", LabelMedium, 白色
  - **选中标记**: ✓, 24.dp, 白色, 右上角

**推荐音色列表**
- 标题: "推荐音色", HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp

**音色卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 深色卡片 (#2A2A2A)
- 内边距: 16.dp
- 边距: 水平16.dp, 底部12.dp
- 布局: 水平排列
- 组件:
  - **音色图标**: 🔊, 24.dp, 主题色
  - **音色信息**:
    - 音色名称: BodyLarge (16.sp), 白色
    - 音色描述: BodySmall (12.sp), 灰色
  - **试听按钮**:
    - 宽度: 64.dp, 高度: 32.dp
    - 圆角: CornerRadius.medium
    - 背景: 主题色
    - 文本: "试听", LabelSmall, 白色
    - 位置: 右侧

**语速调节区域**
- 标题: "语速调节", HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp
- 滑块卡片:
  - 宽度: match_parent - 32.dp
  - 高度: wrap_content
  - 圆角: CornerRadius.large (16.dp)
  - 背景: 深色卡片 (#2A2A2A)
  - 内边距: 20.dp
  - 边距: 水平16.dp
  - 滑块:
    - 范围: 0.5x - 2.0x
    - 默认: 1.0x
    - 颜色: 主题色
    - 标签: 左侧"慢速"，右侧"快速"
    - 当前值: 居中显示，HeadlineMedium

**音量调节区域**
- 标题: "音量调节", HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp
- 滑块卡片: 样式同语速调节
  - 范围: 0% - 100%
  - 默认: 80%
  - 标签: 左侧"小声"，右侧"大声"

---

## 2. 模型设置页面 (Model Settings)

### 设计思路
用户可以配置AI模型的参数，包括模型选择、温度、最大token数等。提供预设配置和自定义选项。

### 布局结构
```
┌─────────────────────────────────┐
│  顶部导航栏                       │
│  ←  模型设置                     │
├─────────────────────────────────┤
│                                 │
│  当前模型                        │
│  ┌─────────────────────────┐   │
│  │ 🤖 GPT-4 Turbo          │   │
│  │ 最新模型，性能强大        │   │
│  │         [✓]             │   │
│  └─────────────────────────┘   │
│                                 │
│  可用模型                        │
│  ┌─────────────────────────┐   │
│  │ 🤖 GPT-3.5 Turbo        │   │
│  │ 快速响应，成本较低        │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 🤖 Claude 3 Opus        │   │
│  │ 理解能力强，适合复杂任务  │   │
│  └─────────────────────────┘   │
│                                 │
│  模型参数                        │
│  ┌─────────────────────────┐   │
│  │ 温度 (Temperature)       │   │
│  │ 创造性 ━━━●━━━━━━ 精确性 │   │
│  │        0.7              │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 最大长度 (Max Tokens)    │   │
│  │ 短 ━━━━━━━●━━━━━━━ 长   │   │
│  │        2048             │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ Top P                   │   │
│  │ 保守 ━━━━━●━━━━━━ 多样  │   │
│  │        0.9              │   │
│  └─────────────────────────┘   │
│                                 │
│  [恢复默认设置]                  │
│                                 │
└─────────────────────────────────┘
```

### 设计细节

**顶部导航栏**
- 样式同朗读音色页面
- 标题: "模型设置"

**当前模型卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 主题色渐变
- 内边距: 20.dp
- 边距: 水平16.dp, 顶部16.dp
- 组件:
  - **模型图标**: 🤖, 32.dp, 白色
  - **模型名称**: HeadlineMedium (18.sp), 白色, 粗体
  - **模型描述**: BodyMedium (14.sp), 白色透明80%
  - **选中标记**: ✓, 24.dp, 白色, 右上角

**可用模型列表**
- 标题: "可用模型", HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp

**模型卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 深色卡片 (#2A2A2A)
- 内边距: 16.dp
- 边距: 水平16.dp, 底部12.dp
- 布局: 水平排列
- 组件:
  - **模型图标**: 🤖, 24.dp, 主题色
  - **模型信息**:
    - 模型名称: BodyLarge (16.sp), 白色
    - 模型描述: BodySmall (12.sp), 灰色
  - **选择按钮**: 单选按钮, 20.dp, 右侧

**模型参数区域**
- 标题: "模型参数", HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp

**参数调节卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 深色卡片 (#2A2A2A)
- 内边距: 20.dp
- 边距: 水平16.dp, 底部12.dp
- 组件:
  - **参数名称**: BodyLarge (16.sp), 白色, 粗体
  - **参数说明**: BodySmall (12.sp), 灰色, 底部间距8.dp
  - **滑块**:
    - 颜色: 主题色
    - 标签: 左右两侧
    - 当前值: 居中显示，HeadlineMedium

**参数说明**:
1. **温度 (Temperature)**:
   - 范围: 0.0 - 2.0
   - 默认: 0.7
   - 说明: 控制输出的随机性，值越高越有创造性
   
2. **最大长度 (Max Tokens)**:
   - 范围: 256 - 4096
   - 默认: 2048
   - 说明: 控制生成文本的最大长度
   
3. **Top P**:
   - 范围: 0.0 - 1.0
   - 默认: 0.9
   - 说明: 控制输出的多样性

**恢复默认按钮**
- 宽度: match_parent - 32.dp
- 高度: 48.dp
- 圆角: CornerRadius.medium (12.dp)
- 背景: 透明，边框1.dp主题色
- 文本: "恢复默认设置", LabelLarge, 主题色
- 边距: 水平16.dp, 顶部24.dp, 底部16.dp

---

## 3. 设置页面 (General Settings)

### 设计思路
通用设置页面，包含通知、隐私、账号、关于等各类设置选项。采用分组卡片布局，清晰分类。

### 布局结构
```
┌─────────────────────────────────┐
│  顶部导航栏                       │
│  ←  设置                         │
├─────────────────────────────────┤
│                                 │
│  通知设置                        │
│  ┌─────────────────────────┐   │
│  │ 🔔 推送通知          [●] │   │
│  │ 📧 邮件通知          [○] │   │
│  │ 🔊 声音提醒          [●] │   │
│  │ 📳 震动反馈          [●] │   │
│  └─────────────────────────┘   │
│                                 │
│  学习设置                        │
│  ┌─────────────────────────┐   │
│  │ 🎯 每日学习目标       >  │   │
│  │ ⏰ 学习提醒时间       >  │   │
│  │ 📊 学习统计显示       >  │   │
│  └─────────────────────────┘   │
│                                 │
│  隐私与安全                      │
│  ┌─────────────────────────┐   │
│  │ 🔒 修改密码           >  │   │
│  │ 👤 隐私设置           >  │   │
│  │ 🛡️ 数据安全           >  │   │
│  └─────────────────────────┘   │
│                                 │
│  账号管理                        │
│  ┌─────────────────────────┐   │
│  │ 📱 绑定手机号         >  │   │
│  │ 📧 绑定邮箱           >  │   │
│  │ 🔗 第三方账号         >  │   │
│  └─────────────────────────┘   │
│                                 │
│  其他                           │
│  ┌─────────────────────────┐   │
│  │ ℹ️ 关于我们           >  │   │
│  │ 📄 用户协议           >  │   │
│  │ 🔄 检查更新           >  │   │
│  │ 🗑️ 清除缓存           >  │   │
│  └─────────────────────────┘   │
│                                 │
│  [退出登录]                      │
│                                 │
└─────────────────────────────────┘
```

### 设计细节

**顶部导航栏**
- 样式同朗读音色页面
- 标题: "设置"

**分组标题**
- 文本: HeadlineSmall (16.sp), 白色, 粗体
- 内边距: 水平16.dp, 顶部24.dp, 底部12.dp
- 分组: 通知设置、学习设置、隐私与安全、账号管理、其他

**设置卡片**
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 深色卡片 (#2A2A2A)
- 边距: 水平16.dp, 底部12.dp

**设置项**
- 高度: 56.dp
- 内边距: 水平16.dp
- 布局: 水平排列
- 分隔线: 0.5.dp, 深灰 (#3A3A3A), 除最后一项
- 组件:
  - **图标**: 24.dp, 主题色, 左侧
  - **标题**: BodyLarge (16.sp), 白色, 图标右侧12.dp
  - **开关/箭头**: 右侧
    - 开关: 48.dp × 28.dp, 主题色
    - 箭头: 20.dp, 灰色

**开关样式**
- 宽度: 48.dp
- 高度: 28.dp
- 圆角: CornerRadius.full
- 背景: 关闭时灰色，开启时主题色
- 滑块: 24.dp圆形, 白色
- 动画: 滑动动画，时长200ms

**退出登录按钮**
- 宽度: match_parent - 32.dp
- 高度: 48.dp
- 圆角: CornerRadius.medium (12.dp)
- 背景: 透明，边框1.dp红色
- 文本: "退出登录", LabelLarge, 红色
- 边距: 水平16.dp, 顶部24.dp, 底部16.dp
- 点击: 显示确认对话框

---

## 交互流程

### 朗读音色设置
1. 用户点击"朗读音色"进入设置页面
2. 查看当前音色和推荐音色列表
3. 点击"试听"按钮播放音色示例
4. 点击音色卡片选择新音色
5. 调节语速和音量
6. 自动保存设置

### 模型设置
1. 用户点击"模型设置"进入设置页面
2. 查看当前模型和可用模型列表
3. 点击模型卡片选择新模型
4. 调节模型参数（温度、最大长度、Top P）
5. 点击"恢复默认设置"重置参数
6. 自动保存设置

### 通用设置
1. 用户点击"设置"进入设置页面
2. 浏览各分组的设置选项
3. 切换开关或点击项进入详细设置
4. 修改设置后自动保存
5. 点击"退出登录"显示确认对话框

---

## 动画规范

### 页面进入动画
```kotlin
// 从右侧滑入
val offsetX by animateFloatAsState(
    targetValue = 0f,
    animationSpec = tween(
        durationMillis = 300,
        easing = FastOutSlowInEasing
    )
)
```

### 开关切换动画
```kotlin
// 滑块滑动动画
val offsetX by animateFloatAsState(
    targetValue = if (isChecked) 20.dp.toPx() else 0f,
    animationSpec = tween(
        durationMillis = 200,
        easing = FastOutSlowInEasing
    )
)

// 背景颜色动画
val backgroundColor by animateColorAsState(
    targetValue = if (isChecked) Primary else Gray,
    animationSpec = tween(200)
)
```

### 试听按钮动画
```kotlin
// 播放时脉冲动画
val scale by infiniteTransition.animateFloat(
    initialValue = 1f,
    targetValue = 1.1f,
    animationSpec = infiniteRepeatable(
        animation = tween(500, easing = FastOutSlowInEasing),
        repeatMode = RepeatMode.Reverse
    )
)
```

---

## 状态管理

### 朗读音色状态
```kotlin
data class VoiceSettings(
    val selectedVoice: Voice,
    val speed: Float = 1.0f,
    val volume: Float = 0.8f
)

data class Voice(
    val id: String,
    val name: String,
    val description: String,
    val gender: Gender,
    val accent: Accent,
    val sampleUrl: String
)

enum class Gender { MALE, FEMALE }
enum class Accent { US, UK, AU }
```

### 模型设置状态
```kotlin
data class ModelSettings(
    val selectedModel: AIModel,
    val temperature: Float = 0.7f,
    val maxTokens: Int = 2048,
    val topP: Float = 0.9f
)

data class AIModel(
    val id: String,
    val name: String,
    val description: String,
    val provider: String
)
```

### 通用设置状态
```kotlin
data class GeneralSettings(
    val pushNotification: Boolean = true,
    val emailNotification: Boolean = false,
    val soundAlert: Boolean = true,
    val vibration: Boolean = true,
    val dailyGoal: Int = 30,
    val reminderTime: String = "20:00"
)
```

---

## 数据持久化

### 使用 DataStore 存储
```kotlin
// 保存设置
suspend fun saveVoiceSettings(settings: VoiceSettings) {
    dataStore.edit { preferences ->
        preferences[VOICE_ID_KEY] = settings.selectedVoice.id
        preferences[VOICE_SPEED_KEY] = settings.speed
        preferences[VOICE_VOLUME_KEY] = settings.volume
    }
}

suspend fun saveModelSettings(settings: ModelSettings) {
    dataStore.edit { preferences ->
        preferences[MODEL_ID_KEY] = settings.selectedModel.id
        preferences[TEMPERATURE_KEY] = settings.temperature
        preferences[MAX_TOKENS_KEY] = settings.maxTokens
        preferences[TOP_P_KEY] = settings.topP
    }
}

suspend fun saveGeneralSettings(settings: GeneralSettings) {
    dataStore.edit { preferences ->
        preferences[PUSH_NOTIFICATION_KEY] = settings.pushNotification
        preferences[EMAIL_NOTIFICATION_KEY] = settings.emailNotification
        preferences[SOUND_ALERT_KEY] = settings.soundAlert
        preferences[VIBRATION_KEY] = settings.vibration
        preferences[DAILY_GOAL_KEY] = settings.dailyGoal
        preferences[REMINDER_TIME_KEY] = settings.reminderTime
    }
}
```

---

## 无障碍支持

### 语义化标签
```kotlin
// 开关
Switch(
    modifier = Modifier.semantics {
        contentDescription = "推送通知，当前${if (checked) "开启" else "关闭"}"
        role = Role.Switch
    }
)

// 滑块
Slider(
    modifier = Modifier.semantics {
        contentDescription = "语速调节，当前${speed}倍速"
    }
)

// 试听按钮
Button(
    modifier = Modifier.semantics {
        contentDescription = "试听${voice.name}音色"
        role = Role.Button
    }
)
```

---

## 实现优先级

### Phase 1 - 基础功能
1. 朗读音色选择
2. 基础模型设置
3. 通用设置开关

### Phase 2 - 完整功能
1. 音色试听功能
2. 语速和音量调节
3. 模型参数调节
4. 所有设置项功能

### Phase 3 - 优化
1. 设置同步
2. 性能优化
3. 无障碍支持
4. 数据备份恢复
