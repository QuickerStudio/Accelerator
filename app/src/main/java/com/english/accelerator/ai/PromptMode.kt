package com.english.accelerator.ai

/**
 * Defines the mode for system prompt selection.
 */
enum class PromptMode {
    /** Use the predefined system prompt for the agent role */
    PRESET,

    /** Use a custom user-defined system prompt */
    CUSTOM
}
