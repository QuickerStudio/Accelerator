# 侧边栏设计 (Sidebar)

## 设计思路

侧边栏是一个从左侧滑出的抽屉式面板，通过手指向右滑动手势触发显示。侧边栏主要用于笔记管理和学习日志查看。顶部是品牌标识和快捷操作按钮，中间是笔记列表和笔记分组，底部是学习日志记录（按时间分组显示）。整个侧边栏支持垂直滚动查看所有内容。

## 功能概览

### 顶部品牌区域
- Accelerator 品牌名称
- 搜索按钮
- 设置按钮

### 新建笔记按钮
- 主题色渐变背景
- ➕ 图标 + "新建笔记"文本

### 全部笔记区域
- 标题："全部笔记"
- 水平滚动列表，显示5个笔记卡片
- 多余的可左右滑动查看

### 笔记分组区域
- 2行网格布局
- 分组图标（📁）
- 可左右滑动查看更多
- ➕ 添加分组按钮

### 单词学习日志
- 标题："单词"
- 按时间分组：
  - 📌 置顶
  - 📅 今天
  - 📅 本周
  - 📅 更早
- 可垂直滚动查看所有日志

---

## 布局结构
```
┌─────────────────────────────────┐
│  顶部品牌区域                     │
│  Accelerator  🔍  ⚙️           │
├─────────────────────────────────┤
│  新建笔记按钮                     │
│  ➕ 新建笔记                     │
├─────────────────────────────────┤
│  全部笔记 (标题)                 │
│  ┌─────────────────────────┐   │
│  │ 笔记1                    │   │
│  │ 笔记2                    │   │
│  │ 笔记3                    │   │
│  │ 笔记4                    │   │
│  │ 笔记5                    │   │
│  └─────────────────────────┘   │
│  (可左右滑动查看更多)             │
├─────────────────────────────────┤
│  笔记分组                        │
│  � 分组1  📁 分组2  ➕         │
│  📁 分组3  📁 分组4             │
│  (显示2行，可左右滑动)            │
├─────────────────────────────────┤
│  单词 (标题)                     │
│                                 │
│  📌 置顶                        │
│  学习日志记录1                   │
│                                 │
│  📅 今天                        │
│  学习日志记录2                   │
│  学习日志记录3                   │
│                                 │
│  📅 本周                        │
│  学习日志记录4                   │
│  学习日志记录5                   │
│                                 │
│  📅 更早                        │
│  学习日志记录6                   │
│  学习日志记录7                   │
│  ...                            │
│  (可垂直滚动查看)                │
└─────────────────────────────────┘
```

---

## 设计细节

### 侧边栏容器
- 宽度: 320.dp (约屏幕宽度的85%)
- 高度: 全屏高度
- 背景: 深色 (#1E1E1E)
- 位置: 从左侧滑出
- 阴影: elevation = 16.dp
- 触发方式: 手指向右滑动手势
- 动画: 滑入/滑出动画，时长300ms
- 可滚动: 垂直滚动查看所有内容

### 遮罩层
- 背景: 黑色半透明 (#000000, 50%透明度)
- 覆盖整个屏幕
- 点击关闭侧边栏
- 动画: 淡入/淡出，时长300ms

### 顶部品牌区域
- 高度: 64.dp
- 背景: 深色 (#1E1E1E)
- 内边距: 水平20.dp
- 布局: 水平排列
- 组件从左到右:
  1. **品牌名称 "Accelerator"**:
     - 文本: HeadlineMedium (20.sp), 白色, 粗体
     - 可选: 品牌Logo图标
  2. **搜索按钮** (�):
     - 尺寸: 40.dp 圆形
     - 图标: 24.dp, 白色
     - 功能: 搜索笔记和学习记录
  3. **设置按钮** (⚙️):
     - 尺寸: 40.dp 圆形
     - 图标: 24.dp, 白色
     - 功能: 打开设置页面

### 新建笔记按钮
- 高度: 56.dp
- 内边距: 水平20.dp, 垂直8.dp
- 圆角: CornerRadius.medium (12.dp)
- 背景: 主题色渐变 (#6366F1 → #8B5CF6)
- 布局: 水平居中
- 图标: ➕, 20.dp, 白色
- 文本: "新建笔记", LabelLarge, 白色
- 图标与文本间距: 8.dp
- 点击: 创建新笔记

### 全部笔记区域
- 标题:
  - 文本: "全部笔记", HeadlineSmall (18.sp), 白色
  - 内边距: 水平20.dp, 垂直12.dp
- 笔记列表:
  - 布局: 水平滚动列表
  - 显示数量: 5个笔记卡片
  - 多余的可左右滑动查看
  - 笔记卡片样式:
    - 尺寸: 宽度120.dp, 高度80.dp
    - 圆角: CornerRadius.medium (12.dp)
    - 背景: 深色卡片 (#2A2A2A)
    - 内边距: 12.dp
    - 标题: BodyMedium (14.sp), 白色, 最多2行
    - 预览: BodySmall (12.sp), 灰色, 最多1行
    - 卡片间距: 12.dp

### 笔记分组区域
- 标题:
  - 文本: "笔记分组", HeadlineSmall (18.sp), 白色
  - 内边距: 水平20.dp, 垂直12.dp
- 分组网格:
  - 布局: 2行网格，可左右滚动
  - 每行显示: 根据内容自适应
  - 分组图标样式:
    - 尺寸: 64.dp × 64.dp
    - 圆角: CornerRadius.medium (12.dp)
    - 背景: 深色卡片 (#2A2A2A)
    - 图标: 📁, 32.dp, 主题色
    - 文本: 分组名称, BodySmall (12.sp), 白色, 居中
    - 图标与文本垂直排列
    - 卡片间距: 12.dp
  - **添加分组按钮** (➕):
    - 样式同分组图标
    - 图标: ➕, 32.dp, 灰色
    - 文本: "添加分组", BodySmall, 灰色
    - 点击: 创建新分组

### 单词区域（学习日志）
- 标题:
  - 文本: "单词", HeadlineSmall (18.sp), 白色
  - 内边距: 水平20.dp, 垂直12.dp
- 学习日志列表:
  - 布局: 垂直列表，可滚动
  - 按时间分组显示
  - 分组标题样式:
    - 高度: 40.dp
    - 内边距: 水平20.dp
    - 图标: 📌/📅, 20.dp, 主题色
    - 文本: BodyLarge (16.sp), 白色, 粗体
    - 背景: 深灰 (#2A2A2A)
  - 日志项样式:
    - 高度: 64.dp
    - 内边距: 水平20.dp
    - 布局: 垂直排列
    - 标题: BodyMedium (14.sp), 白色
    - 详情: BodySmall (12.sp), 灰色
    - 时间: BodySmall (12.sp), 灰色
    - 分隔线: 1.dp, 深灰 (#2A2A2A)

### 学习日志分组
1. **📌 置顶**:
   - 显示置顶的重要学习记录
   - 背景: 深灰 (#2A2A2A)
   
2. **📅 今天**:
   - 显示今天的学习记录
   - 动态更新
   
3. **📅 本周**:
   - 显示本周的学习记录
   - 按日期倒序排列
   
4. **📅 更早**:
   - 显示更早的学习记录
   - 按日期倒序排列
   - 支持无限滚动加载

---

## 交互流程

### 打开侧边栏
1. 用户在屏幕左边缘向右滑动
2. 侧边栏跟随手指滑动
3. 滑动距离 > 屏幕宽度的30%时，松手后自动完全展开
4. 滑动距离 < 屏幕宽度的30%时，松手后自动收回
5. 遮罩层淡入显示
6. 动画时长: 300ms

### 关闭侧边栏
1. 用户点击遮罩层、返回键或向左滑动侧边栏
2. 侧边栏向左滑出
3. 遮罩层淡出消失
4. 动画时长: 300ms

### 新建笔记
1. 用户点击"新建笔记"按钮
2. 侧边栏关闭
3. 打开笔记编辑页面

### 查看笔记
1. 用户点击笔记卡片
2. 侧边栏关闭
3. 打开笔记详情页面

### 管理笔记分组
1. 用户点击分组图标
2. 侧边栏关闭
3. 显示该分组下的所有笔记
4. 用户点击"添加分组"按钮
5. 弹出输入框，输入分组名称
6. 创建新分组

### 查看学习日志
1. 用户滚动到学习日志区域
2. 点击日志项查看详情
3. 支持下拉刷新
4. 支持上拉加载更多

---

## 动画规范

### 侧边栏滑入动画
```kotlin
// 侧边栏跟随手势滑动
val offsetX by animateDpAsState(
    targetValue = if (isOpen) 0.dp else -320.dp,
    animationSpec = tween(
        durationMillis = 300,
        easing = FastOutSlowInEasing
    )
)

// 手势拖动
val dragOffset by remember { mutableStateOf(0f) }
Modifier.draggable(
    state = rememberDraggableState { delta ->
        dragOffset += delta
    },
    orientation = Orientation.Horizontal,
    onDragStopped = { velocity ->
        if (dragOffset > screenWidth * 0.3f) {
            isOpen = true
        } else {
            isOpen = false
        }
    }
)
```

### 遮罩层淡入动画
```kotlin
// 遮罩层透明度动画
val alpha by animateFloatAsState(
    targetValue = if (isOpen) 0.5f else 0f,
    animationSpec = tween(
        durationMillis = 300,
        easing = LinearEasing
    )
)
```

### 笔记卡片点击动画
```kotlin
// 笔记卡片点击缩放动画
val scale by animateFloatAsState(
    targetValue = if (isPressed) 0.95f else 1f,
    animationSpec = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy
    )
)
```

### 新建笔记按钮动画
```kotlin
// 按钮点击动画
val scale by animateFloatAsState(
    targetValue = if (isPressed) 0.95f else 1f,
    animationSpec = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy
    )
)

// 渐变背景动画
val gradientOffset by infiniteTransition.animateFloat(
    initialValue = 0f,
    targetValue = 1f,
    animationSpec = infiniteRepeatable(
        animation = tween(2000, easing = LinearEasing),
        repeatMode = RepeatMode.Reverse
    )
)
```

---

## 状态管理

### 侧边栏状态
```kotlin
data class SidebarState(
    val isOpen: Boolean = false,
    val notes: List<Note> = emptyList(),
    val noteGroups: List<NoteGroup> = emptyList(),
    val learningLogs: Map<LogCategory, List<LearningLog>> = emptyMap(),
    val isLoading: Boolean = false
)

sealed class SidebarEvent {
    object Open : SidebarEvent()
    object Close : SidebarEvent()
    object CreateNote : SidebarEvent()
    data class OpenNote(val noteId: String) : SidebarEvent()
    data class OpenNoteGroup(val groupId: String) : SidebarEvent()
    object CreateNoteGroup : SidebarEvent()
    data class OpenLearningLog(val logId: String) : SidebarEvent()
    object RefreshLogs : SidebarEvent()
    object LoadMoreLogs : SidebarEvent()
}
```

### 数据模型
```kotlin
data class Note(
    val id: String,
    val title: String,
    val preview: String,
    val createdAt: Long,
    val updatedAt: Long
)

data class NoteGroup(
    val id: String,
    val name: String,
    val icon: String,
    val noteCount: Int
)

data class LearningLog(
    val id: String,
    val title: String,
    val content: String,
    val timestamp: Long,
    val category: LogCategory,
    val isPinned: Boolean = false
)

enum class LogCategory {
    PINNED,   // 置顶
    TODAY,    // 今天
    THIS_WEEK, // 本周
    EARLIER   // 更早
}
```

---

## 响应式设计

### 平板/横屏适配
- 宽度: 360.dp (固定宽度)
- 最大宽度: 屏幕宽度的70%
- 保持相同的布局结构

### 小屏幕适配
- 宽度: 屏幕宽度的90%
- 最小宽度: 280.dp
- 字体大小适当缩小
- 笔记卡片宽度调整为100.dp

---

## 无障碍支持

### 语义化标签
```kotlin
// 侧边栏容器
Modifier.semantics {
    contentDescription = "侧边栏，包含笔记和学习日志"
    role = Role.Menu
}

// 新建笔记按钮
Modifier.semantics {
    contentDescription = "新建笔记，点击创建新笔记"
    role = Role.Button
}

// 笔记卡片
Modifier.semantics {
    contentDescription = "笔记：${note.title}，点击查看详情"
    role = Role.Button
}

// 学习日志项
Modifier.semantics {
    contentDescription = "学习日志：${log.title}，时间：${log.timestamp}"
    role = Role.Button
}
```

### 手势导航
- 支持从屏幕左边缘向右滑动打开
- 支持向左滑动关闭
- 支持点击遮罩层关闭
- 支持返回键关闭

---

## 实现优先级

### Phase 1 - 基础功能
1. 侧边栏容器和滑动手势
2. 顶部品牌区域
3. 新建笔记按钮
4. 基础笔记列表

### Phase 2 - 完整功能
1. 笔记分组管理
2. 学习日志列表
3. 搜索功能
4. 下拉刷新和上拉加载

### Phase 3 - 优化
1. 响应式设计
2. 无障碍支持
3. 性能优化
4. 离线缓存
