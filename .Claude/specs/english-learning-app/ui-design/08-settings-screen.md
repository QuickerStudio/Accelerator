# 设置页面设计 (SettingsScreen)

## 设计思路

设置页面（我的页面）是用户的个人中心，顶部显示用户头像和用户名，点击可编辑个人信息。下方是水平滑动的选项卡，展示用户在不同模块的数据统计（收藏、单词、写作、口语、个人简历）。中间是英语学业水平卡片，显示各项学习数据和AI生成的学习建议。底部是功能设置列表，包括任务、主题色、朗读音色、模型设置、设置等选项。整个页面支持垂直滚动。

---

## 布局结构
```
┌─────────────────────────────────┐
│  用户信息区域                     │
│  [头像]  用户名  >               │
├─────────────────────────────────┤
│  选项卡 (水平滑动)                │
│  收藏 | 单词 | 写作 | 口语 | 简历│
├─────────────────────────────────┤
│                                 │
│  英语学业水平卡片                 │
│  ┌─────────────────────────┐   │
│  │ 英语学业水平             │   │
│  │                         │   │
│  │ 学习单词数：000          │   │
│  │ 写作水平：000            │   │
│  │ 口语水平：000            │   │
│  │ 语法水平：000            │   │
│  │                         │   │
│  │ 学习建议：               │   │
│  │ 你需要每日...           │   │
│  │ (AI根据评分自动生成)     │   │
│  └─────────────────────────┘   │
│                                 │
│  功能设置列表                     │
│  🎯 任务                    >   │
│  🎨 主题色                  >   │
│  🔊 朗读音色                >   │
│  🤖 模型设置                >   │
│  ⚙️ 设置                    >   │
│                                 │
├─────────────────────────────────┤
│  底部导航栏                      │
│  单词  写作  对话  设置          │
└─────────────────────────────────┘
```

---

## 设计细节

### 用户信息区域
- 高度: 80.dp
- 背景: 深色 (#121212)
- 内边距: 水平20.dp, 垂直16.dp, 顶部安全区域
- 布局: 水平排列
- 可点击: 进入编辑用户名和头像页面
- 组件:
  - **头像**:
    - 尺寸: 56.dp 圆形
    - 边框: 2.dp, 主题色
    - 默认: 用户首字母或默认头像
  - **用户名**:
    - 字体: HeadlineMedium (20.sp), 白色
    - 位置: 头像右侧，间距12.dp
  - **箭头图标** (>):
    - 尺寸: 20.dp
    - 颜色: 灰色 (#94A3B8)
    - 位置: 最右侧

### 选项卡区域
- 高度: 120.dp
- 背景: 深色 (#121212)
- 内边距: 水平16.dp
- 布局: 水平滚动列表
- 选项卡样式:
  - 宽度: 100.dp
  - 高度: 100.dp
  - 圆角: CornerRadius.large (16.dp)
  - 背景: 深色卡片 (#2A2A2A)
  - 内边距: 12.dp
  - 卡片间距: 12.dp
  - 布局: 垂直居中
  - 标题: BodyMedium (14.sp), 白色
  - 数值: HeadlineMedium (24.sp), 主题色
- 选项卡列表:
  1. 收藏
  2. 单词
  3. 写作
  4. 口语
  5. 个人简历

### 英语学业水平卡片
- 宽度: match_parent - 32.dp
- 高度: wrap_content
- 圆角: CornerRadius.large (16.dp)
- 背景: 深色卡片 (#2A2A2A)
- 阴影: elevation = 4.dp
- 内边距: 20.dp
- 边距: 水平16.dp, 顶部16.dp

**卡片标题**
- 文本: "英语学业水平"
- 字体: HeadlineSmall (18.sp), 白色, 粗体
- 底部间距: 16.dp

**学习数据列表**
- 布局: 垂直排列
- 每项间距: 12.dp
- 数据项样式:
  - 标签: BodyMedium (14.sp), 灰色 (#94A3B8)
  - 数值: HeadlineMedium (20.sp), 白色
  - 布局: 水平排列，标签在左，数值在右
- 数据项:
  1. 学习单词数：000
  2. 写作水平：000
  3. 口语水平：000
  4. 语法水平：000

**学习建议区域**
- 顶部间距: 16.dp
- 顶部分隔线: 1.dp, 深灰 (#3A3A3A)
- 内边距: 顶部16.dp
- 标题: "学习建议", BodyLarge (16.sp), 白色, 粗体
- 建议内容:
  - 字体: BodyMedium (14.sp), 灰色 (#94A3B8)
  - 行高: 22.sp
  - 内容: AI根据用户的学习数据和评分自动生成
  - 示例: "你需要每日坚持学习30分钟，重点提升口语和写作能力。建议多进行对话练习，并完成每日写作任务。"

### 功能设置列表
- 背景: 深色 (#121212)
- 内边距: 水平16.dp, 顶部16.dp
- 设置项样式:
  - 高度: 56.dp
  - 圆角: CornerRadius.medium (12.dp)
  - 背景: 深色卡片 (#2A2A2A)
  - 内边距: 水平16.dp
  - 项间距: 12.dp
  - 布局: 水平排列
  - 图标: 24.dp, 主题色
  - 文本: BodyLarge (16.sp), 白色
  - 图标与文本间距: 12.dp
  - 箭头: 20.dp, 灰色 (#94A3B8), 右对齐
  - 点击效果: 背景变为深灰 (#3A3A3A)

**设置项列表**
1. **任务** (🎯)
   - 功能: 查看和管理学习任务
   
2. **主题色** (🎨)
   - 功能: 选择应用主题颜色
   
3. **朗读音色** (🔊)
   - 功能: 选择TTS朗读音色
   
4. **模型设置** (🤖)
   - 功能: 配置AI模型参数
   
5. **设置** (⚙️)
   - 功能: 应用通用设置

### 底部导航栏
- 高度: 72.dp
- 背景: 深色 (#1E1E1E)
- 4个导航按钮，均匀分布
- 按钮样式:
  - 图标: 24.dp
  - 文字: LabelMedium
  - 未选中: 灰色 (#94A3B8)
  - 选中: 白色
  - 内边距: 垂直12.dp
- 导航项:
  1. 单词 (Vocabulary)
  2. 写作 (Writing)
  3. 对话 (Speaking)
  4. 设置 (Settings) - 当前选中

---

## 交互流程

### 编辑用户信息
1. 用户点击用户信息区域
2. 进入编辑页面
3. 可以修改:
   - 头像（拍照或从相册选择）
   - 用户名（文本输入）
4. 点击保存更新信息

### 查看选项卡数据
1. 用户左右滑动查看不同选项卡
2. 点击选项卡进入详情页面
3. 显示对应模块的详细数据

### 查看学习建议
1. AI根据用户学习数据自动生成建议
2. 每日更新建议内容
3. 建议基于:
   - 学习单词数
   - 写作水平评分
   - 口语水平评分
   - 语法水平评分
   - 学习频率和时长

### 功能设置
1. 用户点击设置项
2. 进入对应的设置页面
3. 修改设置后自动保存

---

## 动画规范

### 用户信息点击动画
```kotlin
val scale by animateFloatAsState(
    targetValue = if (isPressed) 0.95f else 1f,
    animationSpec = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy
    )
)
```

### 选项卡滚动动画
```kotlin
// 水平滚动动画
val scrollState = rememberScrollState()
LazyRow(
    state = scrollState,
    contentPadding = PaddingValues(horizontal = 16.dp),
    horizontalArrangement = Arrangement.spacedBy(12.dp)
)
```

### 设置项点击动画
```kotlin
val backgroundColor by animateColorAsState(
    targetValue = if (isPressed) Color(0xFF3A3A3A) else Color(0xFF2A2A2A),
    animationSpec = tween(200)
)

val scale by animateFloatAsState(
    targetValue = if (isPressed) 0.98f else 1f,
    animationSpec = spring(
        dampingRatio = Spring.DampingRatioMediumBouncy
    )
)
```

### 数据更新动画
```kotlin
// 数值变化动画
val animatedValue by animateIntAsState(
    targetValue = targetValue,
    animationSpec = tween(
        durationMillis = 500,
        easing = FastOutSlowInEasing
    )
)
```

---

## 状态管理

### 用户状态
```kotlin
data class UserState(
    val avatarUrl: String?,
    val username: String,
    val userId: String
)
```

### 学习数据状态
```kotlin
data class LearningStatsState(
    val favoriteCount: Int,
    val wordCount: Int,
    val writingScore: Int,
    val speakingScore: Int,
    val resumeCount: Int,
    val grammarScore: Int,
    val aiSuggestion: String
)
```

### 设置状态
```kotlin
data class SettingsState(
    val currentTheme: ThemeColor,
    val currentVoice: VoiceType,
    val modelConfig: ModelConfig
)
```

---

## AI学习建议生成

### 建议生成逻辑
```kotlin
fun generateLearningSuggestion(stats: LearningStatsState): String {
    val suggestions = mutableListOf<String>()
    
    // 根据单词数量
    if (stats.wordCount < 100) {
        suggestions.add("建议每日学习10-15个新单词")
    }
    
    // 根据写作水平
    if (stats.writingScore < 70) {
        suggestions.add("需要加强写作练习，建议每周完成3篇短文")
    }
    
    // 根据口语水平
    if (stats.speakingScore < 70) {
        suggestions.add("口语需要提升，建议每日进行15分钟对话练习")
    }
    
    // 根据语法水平
    if (stats.grammarScore < 70) {
        suggestions.add("语法基础需要巩固，建议系统学习语法规则")
    }
    
    return suggestions.joinToString("。") + "。"
}
```

---

## 响应式设计

### 平板/横屏适配
- 选项卡显示更多项（不需要滚动）
- 英语学业水平卡片宽度限制为600.dp，居中显示
- 功能设置列表采用2列网格布局

### 小屏幕适配
- 选项卡宽度调整为90.dp
- 字体大小适当缩小
- 保持垂直滚动布局

---

## 无障碍支持

### 语义化标签
```kotlin
// 用户信息区域
Row(
    modifier = Modifier.semantics(mergeDescendants = true) {
        contentDescription = "用户信息，${username}，点击编辑"
        role = Role.Button
    }
)

// 选项卡
Card(
    modifier = Modifier.semantics {
        contentDescription = "收藏数量：${count}"
        role = Role.Button
    }
)

// 设置项
Row(
    modifier = Modifier.semantics(mergeDescendants = true) {
        contentDescription = "任务设置，点击查看任务列表"
        role = Role.Button
    }
)
```

---

## 实现优先级

### Phase 1 - 基础功能
1. 用户信息显示
2. 选项卡数据展示
3. 英语学业水平卡片
4. 基础设置列表

### Phase 2 - 完整功能
1. 编辑用户信息
2. AI学习建议生成
3. 所有设置项功能
4. 数据统计详情页

### Phase 3 - 优化
1. 响应式设计
2. 无障碍支持
3. 性能优化
4. 离线缓存
